{
	"$schema": "https://docs.renovatebot.com/renovate-schema.json",
	"description": "Renovate preset for Python projects using uv",
	"packageRules": [
		{
			"description": "Python dependencies (update pinned versions)",
			"matchManagers": ["pep621"],
			"rangeStrategy": "bump"
		}
	]
}
